import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function ShortlistScreen() {
  return (
    <PlaceholderScreen
      title="Shortlist"
      subtitle="The league table arrives after the core (month 3)."
    />
  );
}
