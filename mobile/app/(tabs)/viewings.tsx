import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function ViewingsScreen() {
  return (
    <PlaceholderScreen
      title="Viewings"
      subtitle="Viewings and the calendar arrive after the core (month 3)."
    />
  );
}
