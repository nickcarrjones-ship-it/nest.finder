import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function MapScreen() {
  return (
    <PlaceholderScreen
      title="Map"
      subtitle="The commute-overlap map lands here — Week 1–2 of the rebuild."
    />
  );
}
